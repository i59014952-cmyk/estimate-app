services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    image: petrovich-proxy:latest
    container_name: petrovich-proxy
    restart: unless-stopped
    expose:
      - "8000"
    environment:
      PORT: "8000"
      ALLOWED_ORIGINS: "*"
      LEMANA_HEADLESS: "0"
      DEFAULT_CITY: "moscow"
    # Browsers (Chromium + Chrome) need extra shared memory or they crash.
    shm_size: "1gb"
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request,sys; sys.exit(0) if urllib.request.urlopen('http://localhost:8000/health').status==200 else sys.exit(1)"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  # Reverse proxy with automatic HTTPS (Let's Encrypt). Needs DOMAIN set to a
  # domain whose A record points at this server, plus ports 80/443 reachable.
  caddy:
    image: caddy:2-alpine
    container_name: caddy
    restart: unless-stopped
    depends_on:
      - backend
    ports:
      - "80:80"
      - "443:443"
    environment:
      DOMAIN: "${DOMAIN:?set DOMAIN in .env, e.g. DOMAIN=petrovich-proxy.ru}"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
      - caddy_config:/config

volumes:
  caddy_data:
  caddy_config:
